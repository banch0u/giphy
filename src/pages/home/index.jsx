import React from 'react';
import Hero from '../../components/hero';
import Feed from '../../components/feed';

const Home = () => {
  return (
    <main>
      <Hero />
      <Feed />
    </main>
  );
};

export default Home;
